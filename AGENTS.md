# Project

Next.js 14, App Router, TypeScript strict, Tailwind CSS,

## Commands
- npm run dev (dev only, never build during coding)
- npm run lint
- npm run test

## Rules
- Server components by default
- "use client" only when truly needed
- No any types
- Tailwind only, no inline styles
- Never touch .env